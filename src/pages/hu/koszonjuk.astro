---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ThanksPixel from '../../components/ThanksPixel.astro';

const locale = 'hu';
const page = 'thanks';
const title = 'Köszönjük! – Content Ninja';
const description = 'Köszönjük a kitöltést! A következő lépés, hogy foglalj egy díjmentes online bemutató időpontot.';
---

<BaseLayout
  locale={locale}
  page={page}
  title={title}
  description={description}
  noindex={true}
  bodyClass="bg-card text-dark font-display antialiased min-h-screen flex flex-col"
>
  <!-- Header (fókuszált – csak logó) -->
  <header class="border-b border-solid border-b-border bg-card sticky top-0 z-50 transition-all">
    <div class="max-w-[1200px] mx-auto px-4 md:px-10 py-4 flex items-center justify-between whitespace-nowrap">
      <div class="flex items-center gap-3 text-dark">
        <a href="/hu/">
          <img src="/assets/logo.webp" alt="Content Ninja logó" class="h-14 md:h-16 w-auto object-contain" />
        </a>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <main class="flex-1 flex flex-col items-center justify-center py-10 px-4">
    <div class="w-full max-w-[1200px]">
      <!-- Unified Card -->
      <div class="bg-card rounded-[16px] border border-border shadow-card overflow-hidden">
        <!-- Card Header -->
        <div class="px-8 md:px-14 pt-10 pb-8 border-b border-border">
          <div class="flex items-center gap-4 md:justify-center">
            <div class="w-11 h-11 rounded-full bg-primary/10 flex items-center justify-center text-primary shrink-0">
              <span class="material-symbols-outlined text-[22px]">calendar_month</span>
            </div>
            <div>
              <h1 class="text-2xl md:text-3xl font-bold text-dark tracking-[-0.02em] leading-tight">
                Válassz egy szabad időpontot 👇
              </h1>
              <p class="text-muted text-sm mt-0.5">Online bemutató – díjmentes, kb. 20 perc</p>
            </div>
          </div>
        </div>

        <!-- Calendar Embed -->
        <div class="px-2 md:px-4 py-4">
          <div
            style="width:100%;min-height:600px;"
            data-fillout-id="3FdjMXSLMuus"
            data-fillout-embed-type="standard"
            data-fillout-inherit-parameters
            data-fillout-dynamic-resize
          >
          </div>
          <script is:inline src="https://server.fillout.com/embed/v1/"></script>
        </div>
      </div>
    </div>
  </main>

  <!-- Footer (fókuszált) -->
  <footer class="py-8 text-center text-muted text-sm border-t border-border bg-card mt-4">
    <p>Copyright © getcontentninja.com | 2026</p>
    <p class="mt-1"><a href="https://app.getcontentninja.com/changelog" class="hover:text-primary transition-colors">Újdonságok</a></p>
  </footer>

  <!-- Lead mérés: a Facebook 'Lead' event csak ?lead=1 esetén tüzel -->
  <ThanksPixel />
</BaseLayout>
