---
import { switchTarget, type Locale, type PageKey } from '../i18n/routes';
import { t } from '../i18n/ui';

interface Props {
  locale: Locale;
  page: PageKey;
  /** Extra osztályok az anchor elemen. */
  class?: string;
  /**
   * Kiírja a nyelv nevét a zászló mellé. A mobil menü kéri: ott a választó a szöveges
   * menüsorok között áll, és egy magányos zászló nem olvasódna menüpontként.
   */
  showLabel?: boolean;
}

const { locale, page, class: className = '', showLabel = false } = Astro.props;
const target = switchTarget(page, locale);
const ui = t(locale);
---

<a
  href={target.path}
  hreflang={target.locale}
  aria-label={ui.langSwitch.aria}
  title={ui.langSwitch.aria}
  class:list={[
    'inline-flex items-center hover:opacity-80 transition-opacity',
    className,
  ]}
>
  <img
    src={`/assets/flags/${ui.langSwitch.flag}.svg`}
    alt={ui.langSwitch.label}
    width="26"
    height="18"
    class="h-[18px] w-auto rounded-[3px] ring-1 ring-black/10 shadow-sm"
  />
  {showLabel && <span class="ml-3 text-sm font-medium text-dark">{ui.langSwitch.label}</span>}
</a>
